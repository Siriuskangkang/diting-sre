# ===== 核心框架 =====
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
langgraph>=0.2.50

# ===== RAG =====
chromadb>=0.5.20
rank-bm25>=0.2.2
sentence-transformers>=3.0.0     # 本地 embedding + reranker (BGE / cross-encoder)
pypdf>=4.0.0                     # PDF 文档加载

# ===== 评估 (L2) =====
ragas>=0.2.6
datasets>=2.20.0

# ===== MCP / 工具调用 (L3) =====
mcp>=1.2.0
httpx>=0.27.0
langchain-mcp-adapters>=0.1.0      # 把 MCP server 工具接入 LangGraph（可选）

# ===== 基础设施适配（可选，配 K8S_IN_CLUSTER=true 时真查 Pod）=====
# kubernetes>=29.0                 # 取消注释以启用真实 k8s 查询

# ===== Multi-Agent 对照 (L4，可选) =====
# crewai>=0.50.0                 # 需要时取消注释

# ===== UI / 脚本 =====
gradio>=4.44.0
typer>=0.12.0
rich>=13.0.0

# ===== 基础 =====
python-dotenv>=1.0.0
pydantic>=2.0.0
